export interface EmulatorStats {
  instructionCount: number;
  ips: number; // instructions per second
}

// These are placeholder types for extending V86 definitions if needed
export type EmulatorInstance = any; 
